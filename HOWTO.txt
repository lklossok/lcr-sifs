### How to use:


## Build Dockerfile
    # 1. Open Docker Desktop
    # 2. Build image from Dockerfile lcr-sifs/dockerfiles/dlbclone/Dockerfile
        {bash}
        cd lcr-sifs/dockerfiles/dlbclone
        docker build -t lklossok/dlbclone:latest .
    # 3. Publish to Docker Hub
        {bash}
        docker push lklossok/dlbclone:latest


## Locally build the Singularity image on thanos:
    # 1. conda activate base
    # 2. Build Singularity image from Docker Hub
        {bash}
        singularity build dlbclone.sif docker://lklossok/dlbclone:latest
    # 3. Test Singularity image R terminal
        {bash}
        singularity exec dlbclone.sif R
        installed.packages()[,c('Package','LibPath')] # check installed R packages and their library paths
    # 4. Publish Singularity image to lcr sifs


## To use the Singularity image on HPC:
    # 1. Open your HPC terminal.
        {bash}
        wget https://github.com/USER/REPO/releases/download/v1.0.0/dlbclone.sif
        singularity exec dlbclone.sif dlbclone


### Current Docker containers available:
    # dlbclone: lklossok/dlbclone:latest
        # Default Kostia Provided ^^

    # GAMBLR: lklossok/gamblr:latest
        # ALL GAMBLR packages ^^